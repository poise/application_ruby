---
#<% require 'poise_boiler' %>
<%= PoiseBoiler.kitchen %>

chef_versions:
- 12

suites:
- name: default
  run_list:
  - recipe[application_ruby_test]
  - recipe[application_ruby_test::bundle_install]
